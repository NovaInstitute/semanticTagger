% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fluree-workflow.R
\name{fluree_tagging_workflow}
\alias{fluree_tagging_workflow}
\title{Start or resume a Fluree-backed tagging workflow}
\usage{
fluree_tagging_workflow(
  config,
  survey_graph,
  tagging_graphs,
  run_id,
  branch = config$branch,
  procedure_id = NULL,
  source_form_id = NULL,
  page_size = 500L,
  batch_size = 250L,
  query_page_size = 100L,
  transaction_delay_seconds = 0,
  base_iri = "https://data.nova.org/tagger/",
  event_callback = NULL
)
}
\arguments{
\item{config}{A Fluree configuration created by \code{\link[novaRush:setConfig]{novaRush::setConfig()}}.}

\item{survey_graph}{Absolute IRI of the novaGraphDB survey named graph.}

\item{tagging_graphs}{Named list of \code{run}, \code{embedding}, \code{hierarchy}, and
\code{review} graph IRIs.}

\item{run_id}{Stable non-empty tagging-run identifier. Reuse it to resume.}

\item{branch}{Fluree branch used for both survey reads and tagging writes.
Defaults to \code{config$branch}.}

\item{procedure_id}{Optional survey-procedure IRI to retain.}

\item{source_form_id}{Optional source-form IRI to retain.}

\item{page_size}{Maximum rows retrieved in each survey query.}

\item{batch_size}{Maximum semantic resources in each tagging write.}

\item{query_page_size}{Maximum records per tagging-state hydration query.}

\item{transaction_delay_seconds}{Seconds to pause after each supporting
Fluree transaction during large writes.}

\item{base_iri}{Base IRI for tagging-domain entities.}

\item{event_callback}{Optional callback passed when starting a new workflow.}
}
\value{
A \code{tagging_workflow}, newly checkpointed or reconstructed from
Fluree.
}
\description{
This is the primary integration entry point for applications. It retrieves
normalized survey questions from a caller-selected survey named graph,
creates a novaRush-backed semantic store for the tagging graphs, and either
starts a new run or resumes the authoritative state already in Fluree.
}
