% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/novarush-semantic-repository.R
\name{novarush_semantic_repository}
\alias{novarush_semantic_repository}
\title{Create a novaRush-backed semantic tagging repository}
\usage{
novarush_semantic_repository(
  config,
  graphs,
  branch = config$branch,
  batch_size = 250L,
  query_page_size = 100L,
  transaction_delay_seconds = 0
)
}
\arguments{
\item{config}{A Fluree configuration created by \code{\link[novaRush:setConfig]{novaRush::setConfig()}}.}

\item{graphs}{Named list containing absolute graph IRIs for \code{run},
\code{embedding}, \code{hierarchy}, and \code{review}.}

\item{branch}{Fluree branch. Defaults to \code{config$branch}.}

\item{batch_size}{Maximum resources in each Fluree write.}

\item{query_page_size}{Maximum records returned by each hydration query.}

\item{transaction_delay_seconds}{Seconds to pause after every supporting
Fluree transaction. This lets background indexing catch up during large
embedding imports. Defaults to zero.}
}
\value{
Repository callbacks accepted by \code{\link[=semantic_tag_store]{semantic_tag_store()}}.
}
\description{
This composition adapter maps novaTagger's transport-free semantic records
to four Fluree named graphs. Embeddings are stored using Fluree's native
vector datatype. Supporting partitions are written in bounded batches and
the mutable run pointer is published last, so an interrupted save cannot
expose an incomplete hierarchy as current.
}
